import React from 'react';
import Profile from '@/components/Profile'; // Adjust the path to where you save the Profile component

const ProfilePage = () => {
  return <Profile />;
};

export default ProfilePage;
